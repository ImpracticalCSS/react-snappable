export interface PaneStyledProps {
    /**
     * The CSS Flex property for determing how the pane will be positioned within the grid.
     */
    flex?: string | number;
}

export interface PaneProps extends PaneStyledProps {
    
}